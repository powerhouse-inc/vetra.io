/** Poll interval (seconds) used when the device flow response omits one. */
export const DEFAULT_POLL_INTERVAL_SECONDS = 5

/** Seconds added to the poll interval when GitHub asks us to slow down. */
export const SLOW_DOWN_BACKOFF_SECONDS = 5
