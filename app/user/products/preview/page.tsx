import { StudioGroupPreview } from '@/modules/cloud/studio/preview/studio-group-preview'

/**
 * IDEATION ONLY — preview route for the Studio group-header layout.
 * Visit /user/products/preview. Mock data, no backend, no auth gate so it's
 * easy to share and review with the devs.
 */
export default function StudioGroupPreviewPage() {
  return <StudioGroupPreview />
}
